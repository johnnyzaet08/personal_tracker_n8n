# Email fixtures

Place sanitized `.eml` samples or normalized `EmailSourceEvent` JSON payloads here only after removing names, addresses, account identifiers, tokens, and financial values that identify a real person.

No fixture is committed in this phase because no representative bank email has been supplied. The local n8n webhook accepts the shared normalized contract and routes it through the same `01 - Email - Router` workflow as Gmail.
